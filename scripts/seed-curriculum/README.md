# scripts/seed-curriculum

Scripts that load the 105-day curriculum into the database go here.

Nothing exists yet. The curriculum is loaded from a data source (e.g. a
structured file or Supabase seed) — it is **never hardcoded in React components**
and no fake data is created during the foundation phases.
